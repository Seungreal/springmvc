<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.cmm.service.CommonMapper">
	<select id="totalCount" parameterType="map" resultType="int">
		${TOTAL_COUNT}
	</select>
	<delete id="dropTable" parameterType="map">${DROP_TABLE}</delete>
	<delete id="createTable" parameterType="map">${CREATE_TABLE}</delete>
	<select id="tableCount" parameterType="map" resultType="int">${TABLE_COUNT}</select>
</mapper>